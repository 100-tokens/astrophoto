# Astrophoto local dev — copy to .env and edit if needed.

# --- Backend ---
APP_BIND=0.0.0.0:8080
APP_LOG=info
APP_DATABASE_URL=postgres://astrophoto:astrophoto@localhost:5434/astrophoto
APP_SESSION_DOMAIN=localhost
APP_SESSION_SECURE=false           # true in prod (HTTPS only)
APP_PUBLIC_BASE_URL=http://localhost:8080

# --- Storage (MinIO local; swap for R2 in prod) ---
APP_S3_ENDPOINT=http://localhost:9100
APP_S3_REGION=us-east-1
APP_S3_BUCKET=astrophoto
APP_S3_ACCESS_KEY=minioadmin
APP_S3_SECRET_KEY=minioadmin
APP_S3_PATH_STYLE=true             # MinIO requires path-style

# --- OAuth (leave blank to disable Google login) ---
APP_OAUTH_GOOGLE_CLIENT_ID=
APP_OAUTH_GOOGLE_CLIENT_SECRET=
APP_OAUTH_GOOGLE_REDIRECT_URL=http://localhost:8080/api/auth/oauth/google/callback

# --- Mail (Phase 8a). Dev: MailHog (no auth). Prod: AWS SES SMTP credentials. ---
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Astrophoto <noreply@astrophoto.local>

# PUBLIC_BASE_URL is what the BACKEND uses to compose links inside outgoing
# emails (reset, email-change confirmation). It must point at the public URL
# that an end-user clicks from their inbox — i.e. the SvelteKit origin.
# This is intentionally separate from APP_PUBLIC_BASE_URL above (which the
# backend exposes for its own self-references). Keep both in sync unless you
# really mean to diverge (e.g. backend behind a different origin than the
# user-facing app).
PUBLIC_BASE_URL=http://localhost:5173

# --- Frontend ---
VITE_API_BASE_URL=http://localhost:8080
PUBLIC_APP_NAME=Astrophoto
